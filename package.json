{
  "version": "1.0.0",
  "name": "rtcts",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "build": "lerna run build",
    "ncu": "lerna exec 'ncu -u --semverLevel major'",
    "ncu:check": "lerna exec 'ncu'"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.22.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint"
    }
  },
  "dependencies": {
    "npm-check-updates": "^7.1.1"
  }
}
